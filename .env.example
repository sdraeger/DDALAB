# DDALAB Configuration Template
#
# This file has been replaced by the unified configuration system.
# 
# 🎯 NEW APPROACH:
#
# 1. Use .env.master as the single source of truth
# 2. Generate environment-specific configurations:
#    npm run deploy:dev      # Development environment
#    npm run deploy:prod     # Production environment
#    npm run config:validate # Validate configuration
#
# 📚 For detailed configuration options, see:
# - .env.master (master template)
# - scripts/config-validator.js (configuration schema)
# - README.md (setup guide)
#
# 🚀 Quick Start:
#
# Development:
#   npm run deploy:dev
#   cd deployments/development-local
#   ./deploy.sh
#
# Production:
#   npm run deploy:prod  
#   cd deployments/production-docker-compose
#   ./deploy.sh
#
# ⚠️  IMPORTANT: This .env.example is kept for compatibility only.
# ⚠️  Use the new deployment system for reproducible configurations.

# Placeholder - use new system instead
DDALAB_ENVIRONMENT=development
DDALAB_DB_USER=admin
DDALAB_DB_PASSWORD=CHANGE_ME
DDALAB_JWT_SECRET_KEY=CHANGE_ME_32_CHARS_MIN
DDALAB_MINIO_ACCESS_KEY=admin
DDALAB_MINIO_SECRET_KEY=CHANGE_ME