# DDALAB Server Configuration
# Copy this file to .env and customize
#
# ============================================================================
# SECURITY WARNING - READ BEFORE PRODUCTION DEPLOYMENT
# ============================================================================
# 1. NEVER commit your actual .env file to version control
# 2. ALL passwords below MUST be changed before production use
# 3. Use strong, unique passwords (minimum 16 characters recommended)
# 4. For HIPAA compliance, ensure audit logging is enabled and logs are secured
# 5. Enable PostgreSQL encryption at rest for sensitive data
# ============================================================================

# Database connection
# SECURITY: Change these credentials before deployment!
DATABASE_URL=postgres://ddalab:CHANGE_THIS_PASSWORD@localhost:5432/ddalab
DB_PASSWORD=CHANGE_THIS_PASSWORD

# Server settings
DDALAB_PORT=8080
DDALAB_BIND_ADDR=0.0.0.0

# Institution information
INSTITUTION_NAME=Research Lab
# SECURITY: Use a strong password (minimum 16 characters, mixed case, numbers, symbols)
BROKER_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# Discovery
ENABLE_MDNS=true

# Security
REQUIRE_AUTH=true
ENABLE_ENCRYPTION=true
SESSION_TIMEOUT_SECONDS=3600
HEARTBEAT_TIMEOUT_SECONDS=300

# Logging
RUST_LOG=ddalab_server=info
