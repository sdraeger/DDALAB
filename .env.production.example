# DDALAB Production Environment Template
# 
# IMPORTANT: Copy this to .env and change ALL passwords!
# 
# Quick Start:
# 1. cp .env.production.example .env
# 2. Edit all CHANGE_ME values below
# 3. docker-compose up -d
# 4. Access at https://localhost

# Docker image (optional - defaults to latest)
# DDALAB_IMAGE=sdraeger1/ddalab:latest

# Database credentials - MUST CHANGE!
DB_USER=ddalab
DB_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD
DB_NAME=ddalab

# MinIO object storage - MUST CHANGE!
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# Security - MUST CHANGE! (minimum 32 characters)
DDALAB_JWT_SECRET_KEY=CHANGE_ME_USE_32_CHAR_RANDOM_STRING_HERE

# Optional: Monitoring credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD

# Optional: Custom domain (default is localhost)
# DDALAB_DOMAIN=your-domain.com

# Optional: Authentication mode
# DDALAB_AUTH_MODE=local  # or ldap, saml